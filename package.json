{
  "name": "eventcatalogpoc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro telemetry disable && eventcatalog dev",
    "build": "eventcatalog build",
    "start": "eventcatalog start",
    "preview": "eventcatalog preview",
    "generate": "eventcatalog generate",
    "postinstall": "astro telemetry disable",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@eventcatalog/core": "2.55.4"
  }
}
